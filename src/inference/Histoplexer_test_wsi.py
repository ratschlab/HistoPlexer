
# prep_process: tissue segmentation, tiling, save tiles temporarily?, resolution of image, what downsample factor, also s.t. ultivue images can be used 
# run inference on tiles, stitch together for wsi
    # also save features from model for downstream tasks 
    # also do s.t can save onl;y specific channels / markers
# no evaluation, just inference 
